# Resolution Tracker

A cross-platform mobile app to track your New Year goals, log progress updates, and celebrate achievements.

## Features

- 📝 Create and manage up to 150 active goals per year
- 📊 Track progress with detailed updates
- 🎉 Celebrate achievements with encouraging messages
- 📈 View progress statistics
- 📋 Generate year-end reports
- 🎯 Goals automatically tied to the current year

## Getting Started

### Prerequisites

- Node.js (v14 or higher)
- npm or yarn
- Expo CLI (`npm install -g expo-cli`)

### Installation

1. Install dependencies:
```bash
npm install
```

2. Start the development server:
```bash
npm start
```

3. Run on your device:
   - Scan the QR code with Expo Go app (iOS/Android)
   - Or press `i` for iOS simulator, `a` for Android emulator

## Project Structure

```
resolutiontracker/
├── App.js                 # Main app entry point
├── src/
│   ├── components/        # Reusable UI components
│   ├── screens/          # Screen components
│   ├── services/         # Data storage and business logic
│   ├── utils/            # Utility functions
│   └── constants/        # App constants
├── assets/               # Images and other assets
└── package.json
```

## License

MIT

